# 项目优化记录

## 1. LangGraph v0.1.0 版本优化

- TTFT：2608.20 ms
- Total：5536.60 ms
- FCP：0.80 s
- LCP：0.80 s

说明：本次记录为优化前基线。TTFT 指从浏览器发起聊天请求到收到第一个非空 SSE 内容的耗时。

### 1. LLM 首 Token 响应时间优化：第一阶段

- 日期：2026-07-18
- 基线 TTFT：2608.20 ms

### 已实施

- 为聊天请求增加分段耗时日志：`request_received`、`conversation_ready`、`user_message_saved`、`history_loaded`、`redis_ready`、`memory_loaded`、`agent_created`、`model_request_started`、`first_model_token`、`first_sse_yielded`。
- Redis 客户端改为应用级懒加载共享实例，移除每次请求前的同步 `ping()`。
- 用户消息保存与历史读取并行执行，历史查询排除当前消息。
- 发送给 Agent 的历史限制为最近 20 条消息且最多 12000 字符。
- SSE 响应增加 `X-Accel-Buffering: no`。
- 修正文档为 UTF-8 编码并统一指标格式。

- 日期：2026-07-18
- 测试次数：10 次成功请求
- 测试方式：使用 `--runs 10 --warmup`，预热请求未计入统计

- 统计结果
- 浏览器端 TTFT 平均值：1295.97 ms
- 模型到后端 SSE 的平均额外耗时：20.32 ms
- 后端 SSE 到浏览器的平均额外耗时：13.42 ms

### 2. TTFT 自动测试脚本

- 日期：2026-07-18
- 脚本：`backend/scripts/measure_ttft.py`

使用方式：

```powershell
python backend/scripts/measure_ttft.py --url http://localhost:8000 --runs 20 --warmup
```

脚本调用 `/api/chat/send`，解析最后一个 `done: true` SSE 事件，并统计 `backend_model_ttft_ms`、`backend_sse_ttft_ms` 和浏览器端 TTFT。失败请求不计入平均值。

### 3. LLM 首 Token 响应时间优化：第二阶段，LLM 客户端连接复用

- 日期：2026-07-18
- 改动：缓存 `ChatOpenAI` 实例，复用其底层 HTTP 客户端。
- 未改动：模型名称、系统提示词、工具集合和 API 协议。
- 代码验证：后端编译、LLM 实例缓存复用检查、统计脚本百分位计算和前端生产构建均通过。

- 日期：2026-07-18
- 测试次数：20 次成功请求
- 测试方式：使用 `--runs 20 --warmup`，预热请求未计入统计

- 统计结果

| 指标 | 平均值 | 中位数 | P95 | 最小值 | 最大值 | 标准差 |
|---|---:|---:|---:|---:|---:|---:|
| `backend_model_ttft_ms` | 1739.94 ms | 1724.80 ms | 2400.52 ms | 1109.71 ms | 2410.40 ms | 419.99 ms |
| `backend_sse_ttft_ms` | 1816.92 ms | 1859.69 ms | 2431.30 ms | 1126.14 ms | 2437.99 ms | 432.98 ms |

- 浏览器端 TTFT 平均值：1823.93 ms
- 模型到后端 SSE 的平均额外耗时：76.98 ms
- 后端 SSE 到浏览器的平均额外耗时：7.01 ms

- 结论

- 本轮平均 `backend_model_ttft_ms` 比此前 10 次测试的 1262.23 ms 高 37.8%，不能证明连接复用带来收益。
- 当前主要瓶颈仍在模型服务或模型请求处理阶段，不在前端 SSE。
- 模型 TTFT 标准差为 419.99 ms，最大值比最小值高 1300.69 ms，存在明显服务端波动。
- 当前应优先检查模型服务排队、网络区域、Prompt 输入长度、工具定义和模型供应商能力，不继续调整前端 SSE。

### 4. 请求画像与工具路由：第一阶段

- 日期：2026-07-19
- 已实施：
  - 将首个模型 Token 到 SSE 的计时点改为 `first_sse_enqueued`，新增 `backend_sse_enqueue_ttft_ms`。
  - 增加历史 Token 数、系统 Prompt Token 数、工具数量、工具定义 Token 数、模型调用次数和工具调用次数。
  - 增加 `knowledge_tools`、`data_tools`、`document_tools`、`code_tools` 和 `notification_tools` 场景工具集合。
  - 使用关键词规则选择工具集合；未匹配时保留完整工具集合，默认行为兼容。
  - 增加应用级共享 `httpx.AsyncClient`，通过 FastAPI 生命周期创建和关闭，并注入 `ChatOpenAI`。
  - 扩展 `measure_ttft.py`，支持 `simple`、`knowledge`、`tool`、`long` 和 `--all-scenarios`。

- 代码验证：Python 编译、指标和工具路由检查、前端生产构建均通过。
- 运行验证：需要重启后端后执行以下命令，当前未将未执行的结果记为性能收益：

```powershell
python backend/scripts/measure_ttft.py --url http://localhost:8000 --all-scenarios --runs 20 --warmup
```

- 新的关键指标为 `backend_sse_enqueue_ttft_ms`。旧的 `backend_sse_ttft_ms` 仍由测试脚本兼容读取，但后续记录应使用新字段。

### 5. LLM 首 Token 响应时间优化：计时口径修正与第一阶段复测

- 日期：2026-07-19
- 修正内容：
  - 将 `backend_sse_enqueue_ttft_ms` 改为从 `model_request_started` 到 `first_sse_enqueued` 计时，避免混入请求前置处理时间。
  - 新增 `backend_model_to_sse_ms`，表示模型首 Token 到 SSE 入队的时间。
  - 新增 `backend_request_to_sse_ms`，表示请求接收到 SSE 入队的总时间。
  - 保留 `backend_sse_ttft_ms` 作为旧客户端兼容别名。
  - 更新 `backend/scripts/measure_ttft.py`，输出模型到 SSE 的独立耗时。
- 回归检查：新增 `backend/tests/test_ttft_metrics.py`，使用 Python 标准库 `unittest`。测试先失败，修改后通过。
- 代码验证：`python backend/tests/test_ttft_metrics.py`、`python -m compileall -q backend/app backend/scripts backend/tests`、`git diff --check` 均通过；前端 `npm run build` 通过。
- 复测方式：后端重启后执行 `python backend/scripts/measure_ttft.py --runs 20 --warmup`。

#### 复测结果

| 指标 | 平均值 | 中位数 | P95 | 最小值 | 最大值 | 标准差 |
|---|---:|---:|---:|---:|---:|---:|
| `backend_model_ttft_ms` | 1970.26 ms | 1844.97 ms | 2780.83 ms | 1311.43 ms | 2787.00 ms | 527.73 ms |
| `backend_sse_enqueue_ttft_ms` | 1970.26 ms | 1844.97 ms | 2780.83 ms | 1311.43 ms | 2787.00 ms | 527.73 ms |

- `backend_model_to_sse_ms` 平均为 0.00 ms，说明当前没有观测到额外的 SSE 入队延迟。
- 浏览器 TTFT 平均为 1996.19 ms，比模型 TTFT 高 25.93 ms，前端、SSE 和传输不是当前主要瓶颈。
- 第一阶段性能目标仍未达成：P50 目标为 `<1200 ms`，实际为 `1844.97 ms`；P95 目标为 `<2000 ms`，实际为 `2780.83 ms`。
- 结论：计时口径已修正，SSE 额外延迟可排除；后续应进入场景化 TTFT 基线，分别测试 `simple`、`knowledge`、`tool` 和 `long`。

#### 场景化 TTFT 基线结果

- 数据来源：`performance/ttft_simple.txt`、`performance/ttft_knowledge.txt`、`performance/ttft_tool.txt`、`performance/ttft_long.txt`。
- `long` 场景仅完成 10 次正式请求，样本不足，不作为正式 P95 基线。

| 场景 | 成功样本 | 平均值 | 中位数 | P95 | 最小值 | 最大值 |
|---|---:|---:|---:|---:|---:|---:|
| `simple` | 20 | 1641.77 ms | 1531.79 ms | 2209.01 ms | 852.61 ms | 2622.26 ms |
| `knowledge` | 20 | 1931.17 ms | 1217.54 ms | 4910.76 ms | 813.90 ms | 7525.40 ms |
| `tool` | 20 | 1182.26 ms | 1151.45 ms | 1503.35 ms | 839.83 ms | 1528.69 ms |
| `long` | 10 | 2117.76 ms | 2010.47 ms | 未作正式统计 | 1580.62 ms | 3034.49 ms |

- `tool` 场景表现最好，平均 TTFT 为 1182.26 ms，接近 P50 目标 `<1200 ms`，P95 为 1503.35 ms。
- `simple` 场景仍携带 5 个工具定义，说明简单问题的无工具直连路径尚未生效，是下一步首要优化点。
- `knowledge` 场景中位数为 1217.54 ms，但存在多个极端慢样本，平均值和 P95 被明显拉高，表现为模型服务长尾波动。
- `long` 场景平均 TTFT 为 2117.76 ms，仅作为长上下文场景观察结果，不用于正式目标判定。
- 四个场景的 `backend_model_to_sse_ms` 均为 0.00 ms，继续排除 SSE 转发作为主要瓶颈。
- `tool` 场景反而快于 `simple`，当前不能简单将 TTFT 归因于工具数量；仍需结合模型服务排队和请求内容进一步判断。

#### 第二阶段结论

- 场景化基线已建立，SSE 额外延迟已排除。
- 下一步优先实现 `simple_chat -> ChatOpenAI.stream` 的无工具路径，再用 `simple` 场景重新进行 20 次对比测试。

#### 第三阶段：简单问题直连模型路径

- 日期：2026-07-19
- 修改内容：
  - 未命中特定工具意图时，不再默认创建携带 5 个工具的 `AgentExecutor`。
  - 普通问题改为通过 `get_llm().astream` 直接调用 Chat Model。
  - 知识库、数据库、Python、邮件等明确工具意图仍保留 Agent 路径。
  - 直连路径继续复用系统提示词、历史消息和现有 SSE 格式。
- 回归验证：`backend/tests/test_ttft_metrics.py` 2 个测试通过，`python -m py_compile backend/app/services/chat_service.py` 通过，`git diff --check` 通过。
- 单次实际验证：`simple` 场景返回 `tools=0`，`backend_model_ttft_ms=1400.10 ms`，说明无工具直连路由已生效。
- 本次单次请求的客户端首事件计时为 12818.38 ms，与后端模型 TTFT 不一致，属于单次异常观测，暂不作为性能结论，需后续 20 次复测确认。

##### 20 次复测对比

| 指标 | 原 Agent 路径 | 无工具直连路径 | 改善 |
|---|---:|---:|---:|
| `backend_model_ttft_ms` 平均值 | 1641.77 ms | 1102.12 ms | 32.9% |
| `backend_model_ttft_ms` 中位数 | 1531.79 ms | 1055.32 ms | 31.1% |
| `backend_model_ttft_ms` P95 | 2209.01 ms | 1515.90 ms | 31.4% |
| `backend_model_ttft_ms` 标准差 | 453.26 ms | 253.01 ms | 44.2% |
| 浏览器 TTFT 平均值 | 1780.87 ms | 1210.63 ms | 32.0% |

- 测试方式：`simple` 场景，20 次成功请求，1 次预热请求不计入统计。
- 结论：无工具直连路径满足“简单问题 TTFT 至少比完整 Agent 路径低 20%”的验收要求，实际后端平均 TTFT 降低 32.9%。
- 简单问题 P95 已达到 `<1500 ms` 的目标附近，但实测为 1515.90 ms，仍差 15.90 ms；中位数 1055.32 ms 已达到 `<1200 ms` 目标。

#### 第四阶段：系统 Prompt A/B 准备

- 日期：2026-07-19
- 新增 `PROMPT_MODE` 配置：`full` 为默认完整提示词，`compact` 为紧凑提示词实验版本。
- 紧凑版本保留中文/Markdown 输出、事实来源与反幻觉、业务边界、工具调用和回答结构约束，删除重复表述与冗长示例。
- `SIMPLE_CHAT_ENABLED` 现接入实际路由，默认值设为 `true`；可通过环境变量回退到完整 Agent 路径。
- 同步更新 `backend/app/config.py` 和 `backend/.env.example`。
- 验证：`backend/tests/test_ttft_metrics.py` 3 个测试通过，`python -m compileall -q backend/app backend/scripts backend/tests` 通过，`git diff --check` 通过。
- 当前默认仍为 `PROMPT_MODE=full`。在将 `PROMPT_MODE=compact` 设为默认前，需要分别比较 TTFT、工具选择正确率、格式合规率和事实错误率。

##### Compact Prompt TTFT 对比

| 场景 | Full 平均值 | Compact 平均值 | Full 中位数 | Compact 中位数 | 结论 |
|---|---:|---:|---:|---:|---|
| `simple` | 1102.12 ms | 1261.46 ms | 1055.32 ms | 1121.06 ms | 变慢 14.5% |
| `tool` | 1182.26 ms | 1365.18 ms | 1151.45 ms | 1073.07 ms | 平均变慢 15.5%，P95 上升至 2448.07 ms |
| `knowledge` | 1931.17 ms | 3630.85 ms | 1217.54 ms | 4304.69 ms | 明显变慢，长尾恶化 |

- 三个场景均完成 20 次成功请求，`backend_model_to_sse_ms` 仍为 0.00 ms。
- Compact Prompt 未带来 TTFT 收益，且 `knowledge` 场景的平均值、中位数和 P95 均显著恶化。
- 结论：本轮 A/B 未通过，维持 `PROMPT_MODE=full`，不将紧凑 Prompt 设为默认，也不继续投入应用层 Prompt 压缩优化。
- 代码处理：紧凑 Prompt 实现、`PROMPT_MODE` 配置及相关测试已回退；完整 Prompt 恢复为唯一实现。真实 `backend/.env` 未由本次回退修改。
